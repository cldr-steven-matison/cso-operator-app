# Mac dev overrides — assumes scripts/mac-dev.sh port-forwards.
# Copy to .env.local to activate.

VLLM_URL=http://localhost:8000
QDRANT_URL=http://localhost:6333
EMBED_URL=http://localhost:8080
WHISPER_URL=http://localhost:8001

# NiFi: port-forward svc/<nifi-service> in cfm-streaming namespace, then point here.
NIFI_URL=https://localhost:8443

# Kafka: port-forward svc/my-cluster-kafka-bootstrap 9092:9092 -n cld-streaming.
KAFKA_BOOTSTRAP=localhost:9092
